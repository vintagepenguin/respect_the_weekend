<div class="top-div">
  <div class="main-title">
      <h1>respect <span>the</span> weekend.</h1>
      <h4>ONLY UNIQUE EVENTS. ONLY WEEKEND EVENTS.</h4>
  </div>

    <div class="top-links">
      <div class="create">
        <p><%= link_to "Create An Event", new_event_path  %></p>
      </div>

      <div id="user_nav">
          <% if current_user %>
         <p><span>Oh hey</span><strong> <%= current_user.name  %> </strong>
            <%= link_to "Sign Out", signout_path, id: "sign_out" %>
          </p>   
            <% else %>
          <p>
            <%= link_to "Sign In With Facebook", "/auth/facebook", id: "sign_in" %>
          </p>
          <% end  %>
      </div>
<!--   <div class="tag-line">
    <h3>the weekend comes but once a week.</h3>
  </div> -->
  </div>  
</div>